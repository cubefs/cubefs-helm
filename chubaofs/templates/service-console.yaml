{{- if .Values.component.monitor }}
{{- $envAll := . }}

---
apiVersion: v1
kind: Service
metadata:
  name: console-service
  
spec:
  ports:
    - port: {{ .Values.console.port }}
      protocol: TCP
      targetPort: {{ .Values.console.port }}
  selector:
{{ tuple $envAll .Release.Namespace "console" | include "helm-toolkit.metadata_labels" | indent 4 }}
{{ end }}